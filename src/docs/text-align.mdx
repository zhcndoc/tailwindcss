import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "text-align";
export const description = "用于控制文本对齐的工具类.";

<ApiTable
  rows={[
    ["text-left", "text-align: left;"],
    ["text-center", "text-align: center;"],
    ["text-right", "text-align: right;"],
    ["text-justify", "text-align: justify;"],
    ["text-start", "text-align: start;"],
    ["text-end", "text-align: end;"],
  ]}
/>

## 示例

### 左对齐文本

使用 `text-left` 工具类将元素的文本左对齐：

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-left">
          所以我开始走进水里。我不会对你们撒谎，伙计们，我很害怕。但我坚持下来了，当我越过海浪时，一种奇怪的平静涌上心头。我不知道那是神的干预还是所有生物之间的亲情，但我告诉你，杰瑞，那一刻，我<em>是</em>一名海洋生物学家。
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-left] -->
<p class="text-left">我开始走进水里...</p>
```

</Figure>

### 右对齐文本

使用 `text-right` 工具类将元素的文本右对齐：

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-right">
          所以我开始走进水里。我不会对你们撒谎，伙计们，我很害怕。但我坚持下来了，当我越过海浪时，一种奇怪的平静涌上心头。我不知道那是神的干预还是所有生物之间的亲情，但我告诉你，杰瑞，那一刻，我<em>是</em>一名海洋生物学家。
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-right] -->
<p class="text-right">我开始走进水里...</p>
```

</Figure>

### 居中对齐文本

使用 `text-center` 工具类将元素的文本居中对齐：

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-center">
          所以我开始走进水里。我不会对你们撒谎，伙计们，我很害怕。但我坚持下来了，当我越过海浪时，一种奇怪的平静涌上心头。我不知道那是神的干预还是所有生物之间的亲情，但我告诉你，杰瑞，那一刻，我<em>是</em>一名海洋生物学家。
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-center] -->
<p class="text-center">我开始走进水里...</p>
```

</Figure>

### 两端对齐文本

使用 `text-justify` 工具类将元素的文本两端对齐：

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-justify">
          所以我开始走进水里。我不会对你们撒谎，伙计们，我很害怕。但我坚持下来了，当我越过海浪时，一种奇怪的平静涌上心头。我不知道那是神的干预还是所有生物之间的亲情，但我告诉你，杰瑞，那一刻，我<em>是</em>一名海洋生物学家。
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code classes:text-justify] -->
<p class="text-justify">所以我开始走进水里......</p>
```

</Figure>

### 使用逻辑属性

使用 `text-start` 和 `text-end` 工具类，这些工具类使用[逻辑属性](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Logical_Properties/Basic_concepts)，根据文本方向映射到左侧或右侧：

<Figure>

<Example padding={false}>
  {
    <div className="px-4 sm:px-0" dir="rtl" lang="ar">
      <div className="mx-auto max-w-lg p-8 text-sm/6 text-gray-900 dark:text-gray-200">
        <p className="text-end">
          بدأتُ أسير نحو الماء. لن أكذب عليكم يا رفاق، كنتُ مرعوبًا. لكنني واصلتُ المسير، وبينما كنتُ أشق طريقي عبر
          الأمواج، غمرني هدوءٌ غريب. لا أعلم إن كان ذلك تدخّلًا إلهيًا أم صلة قرابة بين جميع الكائنات الحية، لكنني أقول
          لك يا جيري، في تلك اللحظة، كنتُ عالم أحياء بحرية.
        </p>
      </div>
    </div>
  }
</Example>

```html
<!-- [!code word:dir="rtl"] -->
<!-- [!code classes:text-end] -->
<div dir="rtl" lang="ar">
  <p class="text-end">فبدأت بالسير نحو الماء...</p>
</div>
```

</Figure>

### 响应式设计

<ResponsiveDesign element="p" property="text-align" defaultClass="text-left" featuredClass="text-center" />