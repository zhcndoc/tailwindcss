import { ApiTable } from "@/components/api-table.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "break-inside";
export const description = "用于控制元素内部如何进行列或页面换行的工具。";

<ApiTable
  rows={[
    ["break-inside-auto", "break-inside: auto;"],
    ["break-inside-avoid", "break-inside: avoid;"],
    ["break-inside-avoid-page", "break-inside: avoid-page;"],
    ["break-inside-avoid-column", "break-inside: avoid-column;"],
  ]}
/>

## 示例

### 基本示例

使用 `break-inside-column` 和 `break-inside-avoid-page` 等工具来控制元素内部列或页面换行的行为：

```html
<!-- [!code classes:break-inside-avoid-column] -->
<div class="columns-2">
  <p>好吧，让我告诉你一些事情，...</p>
  <p class="break-inside-avoid-column">当然，继续，笑吧...</p>
  <p>也许我们可以没有...</p>
  <p>看。如果你认为这是...</p>
</div>
```

### 响应式设计

<ResponsiveDesign property="break-inside" defaultClass="break-inside-avoid-column" featuredClass="break-inside-auto" />
