import { ApiTable } from "@/components/api-table.tsx";
import { Example } from "@/components/example.tsx";
import { Figure } from "@/components/figure.tsx";
import { ResponsiveDesign } from "@/components/content.tsx";

export const title = "word-break";
export const description = "控制元素中文字断开的工具类。";

<ApiTable
  rows={[
    ["break-normal", "overflow-wrap: normal;\nword-break: normal;"],
    ["break-words", "overflow-wrap: break-word;"],
    ["break-all", "word-break: break-all;"],
    ["break-keep", "word-break: keep-all;"],
  ]}
/>

## 示例

### 正常

使用 `break-normal` 工具类仅在正常单词断点处添加换行：

<Figure>

<Example padding={false}>
  {
    <p className="mx-auto max-w-xs border-x border-x-pink-400/30 py-8 break-normal text-gray-900 dark:text-gray-200">
      在任何主要英语字典中最长的单词是{" "}
      <span className="font-bold">pneumonoultramicroscopicsilicovolcanoconiosis,</span> 这个词指的是一种因吸入非常细小的硅颗粒而感染的肺病，尤其是来自火山的颗粒；在医学上，这和硅肺病是相同的。
    </p>
  }
</Example>

```html
<!-- [!code classes:break-normal] -->
<p class="break-normal">在任何主要英语字典中最长的单词是...</p>
```

</Figure>

### 断字

使用 `break-words` 工具类在需要时在单词中间添加换行：

<Figure>

<Example padding={false}>
  {
    <p className="mx-auto max-w-xs border-x border-x-pink-400/30 py-8 break-words text-gray-900 dark:text-gray-200">
      在任何主要英语字典中最长的单词是{" "}
      <span className="font-bold">pneumonoultramicroscopicsilicovolcanoconiosis,</span> 这个词指的是一种因吸入非常细小的硅颗粒而感染的肺病，尤其是来自火山的颗粒；在医学上，这和硅肺病是相同的。
    </p>
  }
</Example>

```html
<!-- [!code classes:break-words] -->
<p class="break-words">在任何主要英语字典中最长的单词是...</p>
```

</Figure>

### 断行所有

使用 `break-all` 工具类在必要时随意添加换行，而不尝试保留完整的单词：

<Figure>

<Example padding={false}>
  {
    <p className="mx-auto max-w-xs border-x border-x-pink-400/30 py-8 break-all text-gray-900 dark:text-gray-200">
      在任何主要英语字典中最长的单词是{" "}
      <span className="font-bold">pneumonoultramicroscopicsilicovolcanoconiosis,</span> 这个词指的是一种因吸入非常细小的硅颗粒而感染的肺病，尤其是来自火山的颗粒；在医学上，这和硅肺病是相同的。
    </p>
  }
</Example>

```html
<!-- [!code classes:break-all] -->
<p class="break-all">在任何主要英语字典中最长的单词是...</p>
```

</Figure>

### 保持断行

使用 `break-keep` 工具类防止对中文/日文/韩文 (CJK) 文本应用换行：

<Figure>

<Example padding={false}>
  {
    <p className="mx-auto max-w-xs border-x border-x-pink-400/30 py-8 break-keep text-gray-900 dark:text-gray-200">
      抗衡不屈不挠 (kànghéng bùqū bùnáo) 这是一个长词，意思是不畏强暴，奋勇抗争，坚定不移，永不放弃。
      <span className="font-bold">这个词通常用来描述那些在面对困难和挑战时坚持自己信念的人，</span>{" "}
      他们克服一切困难，不屈不挠地追求自己的目标。无论遇到多大的挑战，他们都能够坚持到底，不放弃，最终获得胜利。
    </p>
  }
</Example>

```html
<!-- [!code classes:break-keep] -->
<p class="break-keep">抗衡不屈不挠...</p>
```

</Figure>

对于非 CJK 文本，`break-keep` 工具类的行为与 `break-normal` 工具类相同。

### 响应式设计

<ResponsiveDesign element="p" property="word-break" defaultClass="break-normal" featuredClass="break-all" />
